{
  "name": "frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:production": "vite build --mode production",
    "build:staging": "vite build --mode staging",
    "build:client": "vite build --mode client",
    "deploy:production": "npm run build:production && firebase deploy --project production",
    "deploy:staging": "npm run build:staging && firebase deploy --project staging",
    "deploy:client": "npm run build:client && firebase deploy --project client",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.1.0",
    "papaparse": "^5.5.3",
    "pinia": "^3.0.4",
    "rodnecislo": "^1.8.0",
    "validator": "^13.15.26",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "jiti": "^2.4.2",
    "vite": "^7.0.6",
    "vite-plugin-vue-devtools": "^8.0.0"
  }
}
